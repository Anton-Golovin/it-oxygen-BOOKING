const loadingStyles = () => ({
  loading: {
    background: 'rgba(256, 256, 256, 0.5)',
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
  },
  request: {
    position: 'fixed',
    top: '50%',
  },
});

export default loadingStyles;
